<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="ehcache.xsd" updateCheck="true"
	monitoring="autodetect" dynamicConfig="true">

	<!-- By default, Ehcache stored the cached files in temp folder. -->
	<!-- <diskStore path="java.io.tmpdir" /> -->
	
	<!-- Ask Ehcache to store cache in this path -->
	<diskStore path="c:\\cache" />

	<!-- Sample cache named cache1
    This cache contains a maximum in memory of 10000 elements, and will expire
    an element if it is idle for more than 5 minutes and lives for more than
    10 minutes.

    If there are more than 10000 elements it will overflow to the
    disk cache, which in this configuration will go to wherever java.io.tmp is
    defined on your system. On a standard Linux system this will be /tmp" -->
    
    
     <cache alias="pokedexCache" >
		<key-type>java.lang.String</key-type>
		<value-type>java.util.Collection</value-type>
		<integration>com.revature.pokemonv2.utilities.PokedexLoadWriter</integration>
		<expiry>
			<timeToIdleSeconds>60</timeToIdleSeconds>
		</expiry>
	</cache> 
	
	
	<cache alias ="allPokemonCache">
		<heap>151</heap>
		<key-type>java.lang.Integer</key-type>
		<value-type>com.revature.pokemonv2.model.Pokemon</value-type>
		<integration>com.revature.pokemonv2.utilities.AllPokemonLoadWriter</integration>
		
	</cache>

</ehcache>